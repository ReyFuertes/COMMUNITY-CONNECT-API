// No specific data model for settings as a whole yet.
// This file serves as a placeholder for future setting models.
export interface AppSettings {
  // Example:
  // notificationsEnabled: boolean;
  // theme: string;
}